---
# meta file for while_true_do.sys_firewalld
galaxy_info:
  role_name: sys_firewalld
  author: while-true-do.io
  description: An Ansible role to install and configure firewalld.
  license: BSD

  min_ansible_version: 2.7

  # Supported platforms: https://galaxy.ansible.com/api/v1/platforms/
  # Please also maintain
  # - molecule/default/molecule.yml
  # - molecule/default/tests/test_default.py
  # .travis.yml
  platforms:
    - name: EL
      versions:
        - 7
        - 8

    - name: Fedora
      versions:
        - 29
        - 30

  # Some very common tags are:
  # system, virtualization, development, monitoring, packaging, container
  # configuration, cloud, security
  galaxy_tags:
    - firewalld
    - compliance
    - configuration
    - package
    - port
    - security
    - service
    - system

dependencies: []
